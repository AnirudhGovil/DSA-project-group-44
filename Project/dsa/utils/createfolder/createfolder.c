#include "createfolder.h"

int createfolder(char **args)
{
    /* char cmd1[10001];
    char cmd2[10001];
    sprintf(cmd1,"mkdir %s",args[1]);
    system(cmd1);
    sprintf(cmd2,"cp -r %s .",args[1]);
    system(cmd2);
    return 0; */
}